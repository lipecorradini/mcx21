#include <bits/stdc++.h>

using namespace std;

int main() {
   ios::sync_with_stdio(false);
   cin.tie(0);
    long long a, b;
    int n;
    cin >> n;

    for(int i = 0; i < n; i++){
        cin >> a >> b;
        cout << a + b << '\n';
    }

}
