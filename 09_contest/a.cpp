#include <bits/stdc++.h>

using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    string v;
    while(cin >> v){
        int tam = v.length();
        int last = tam - 1; int first = tam - 2;

        char last_char = v[last];

        while(v[first] != )

    }

}
